tinymce.PluginManager.add('filemanager', function (editor, url) {

    var openDialog = function () {
        return editor.windowManager.openUrl({
            title: 'filemanager plugin',
            url: editor.settings.filemanager_path + (editor.settings.filemanager_path.indexOf('?') > -1 ? '&opener=tinymce' : '?opener=tinymce'),
            width: 1024,
            height: 560
        });
    };

    // Add a button that opens a window
    editor.ui.registry.addButton('filemanager', {
        icon: 'browse',
        text: 'File Manager',
        onAction: function () {
            // Open window
            openDialog();
        }
    });

    // Adds a menu item, which can then be included in any menu via the menu/menubar configuration
    editor.ui.registry.addMenuItem('filemanager', {
        text: 'filemanager plugin',
        onAction: function () {
            // Open window
            openDialog();
        }
    });
});