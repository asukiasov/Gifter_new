@model BlogPropertiesModel.BlogPropertiesViewModel
<div class="actions-row">
    <button type="submit" class="btn btn-circle btn-primary js-save-button" form="BlogDetailsForm"><i class="fas fa-save"></i></button>
</div>
<div class="card">
    <div class="card-body">
        <form id="BlogDetailsForm" method="post" enctype="multipart/form-data">
            <div class="row">
                <div class="form-group col-lg-6">
                    <label>Blog Title</label>
                    <input type="text" name="@nameof(Model.BlogTitle)" class="form-control" value="@Model.BlogTitle" />
                    <p class="help-block">
                        <span class="error-text"></span>
                    </p>
                </div>
                <div class="form-group col-lg-6">
                    <label>Blog Slug</label>
                    <input type="text" name="@nameof(Model.BlogSlug)" class="form-control js-slug-textbox" value="@Model.BlogSlug" />
                    <p class="help-block">
                        <span class="error-text"></span>
                    </p>
                </div>
            </div>
            <div class="row">
                <div class="form-group col-lg-6">
                    <label>Blog Author Name</label>
                    <input type="text" name="@nameof(Model.BlogAuthorName)" class="form-control" value="@Model.BlogAuthorName" />
                </div>
                <div class="form-group col-lg-6">
                    <label>Blog Date</label>
                    @(Html.DevExtreme().DateBox()
                        .ElementAttr("class", "form-control")
                        .Name(nameof(Model.BlogDate))
                        .Value(Model.BlogDate)
                        .Type(DateBoxType.Date)
                        .DisplayFormat(Constants.Formats.Date)
                        .Height(34))
                    <p class="help-block">
                        <span class="error-text"></span>
                    </p>
                </div>
                <div class="form-group col-lg-12">
                    <label>Upload Image</label>
                    <div class="input-group custom-file-group js-custom-file-upload">
                        <div class="form-control">
                            <a class="js-attachment js-image-attachment" href="@Html.Raw(Model.BlogImageHttpPath)">@Html.Raw(Model.BlogImageFilename)</a>
                            <span class="js-new-attachment hidden"></span>
                        </div>
                        <span class="input-group-btn">
                            <button type="button" class="btn btn-warning hidden js-clear-button"><i class="fa fa-times"></i></button>
                            @if (Model.HasBlogImage)
                            {
                                <button type="button" data-url="@Html.Raw(Model.UrlDeleteImage)" data-text-confirm="@Html.Raw(Model.TextConfirmDelete)" class="btn btn-danger js-delete-button"><i class="fas fa-trash-alt"></i></button>
                            }
                            <span class="btn btn-default custom-file-upload">
                                <i class="fas fa-folder-open"></i>
                                <input type="file" name="@nameof(Model.PostedFile)" accept="" />
                            </span>
                        </span>
                    </div>
                    <span class="help-block"></span>
                </div>
            </div>
            <div class="row">
                <div class="form-group col-lg-12">
                    <label>Blog Text</label>
                    <textarea name="@nameof(Model.BlogText)" class="form-control js-apply-tinymce">@Html.Raw(Model.BlogText)</textarea>
                </div>

            </div>
        </form>

    </div>
</div>
@section FooterSection{
    <script src="~/admin/js/Blog/BlogItem.js"></script>
    @if (Model.HasErrors)
    {
        <script>
            Validation.Init({ErrorsJson:@Html.Raw(Model.ErrorsJson)}).ShowErrors();
        </script>
    }
}