@model UsersModel.PageViewModel
@if (Model.ShowAddNewButton)
{
    <div class="actions-row">
        <button class="btn btn-oval btn-default js-add-new-button"><i class="fas fa-plus"></i> <sapn>Add</sapn></button>
    </div>
}
<div class="row">
    <div class="col">
        @await Html.PartialAsync(ViewNames.Admin.Shared.DevexpressGrid, Model.Grid)
    </div>
</div>
@section FooterSection{
    <script src="~/admin/js/UserManagement/Users.js"></script>
}